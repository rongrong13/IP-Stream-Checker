# Changelog

本文件记录项目的主要更改。

## [1.2.0] - 2026-08-13

### 🐛 正确性修复

- **缓存键纳入检测选项**: 修复不同检测设置(风险过滤阈值/数据源/跳过关键词)的请求互相覆盖缓存结果的问题(此前输出订阅可能因共享缓存而错误)
- **取消/重跑竞态**: 提交新任务时,队列中残留的旧任务会被丢弃(按 request_id 校验),避免重复检测与状态错乱
- **SSE 长连接悬挂**: 取消/出错后 `/status/stream` 立即断开,不再无限挂起占用连接
- **内存增长控制**: 任务状态字典加容量上限(100),无人订阅时任务日志限制 200 条,防止长期运行内存持续增长
- **降级不再白等失效源**: ipapi 失败后自动降级只回落到当前可用源,不再依次等待已被 Cloudflare 拦截的 ping0 与已失效的 ippure(每节点最多省 30-45s)

### ✨ 性能优化(借鉴 subs-check)

- **节点测活**: 正式检测前用 Mihomo 内核并发测活(generate_204),不可用节点不进入 IP/流媒体检测阶段,大幅加速大订阅;死节点可"跳过不标注"或"从输出移除"(可配置)
- **打乱测试顺序**: 可选打乱节点测试先后(避免相邻节点扎堆同一时间窗口),输出订阅保持原序
- **ip-api 限速重试**: 429/网络错误时最多重试 2 次(退避 1s/2s),缓解免费版 45 次/分钟限流
- **ip-api 评分增强**: 扩充云厂商 ASN 与机房关键词库,新增住宅宽带 ISP 关键词覆盖(降低家宽误判为机房)

### 🔐 安全

- **可选访问鉴权**: 设置环境变量 `API_TOKEN` 后,所有 API 需 `Authorization: Bearer <token>`(SSE 与订阅链接走 query token);未设置时行为完全不变

### 🛠 工程化

- **依赖固定版本**: requirements.txt 全部锁定精确版本,Dockerfile Go 版本固定 golang:1.26
- **单元测试**: 新增 `tests/`(评分、节点名格式化、缓存键、数据源顺序、日志上限),CI 增加 pytest 步骤

---

## [1.1.0] - 2026-01-11

### ✨ 新增功能
- **Mihomo内核更新**: 更新至v1.19.18
- **停止检测功能**: 新增"停止"按钮，允许用户随时中断正在进行的检测任务
- **Request ID 机制**: 为每次检测生成唯一标识符，防止取消操作的竞态条件
- **共享设备数显示**: Ping0 检测结果现在会在日志中显示"共享: N"信息
- **自引用 URL 解包**: 自动识别并解包 `http://my-site/check?url=...` 格式的递归链接
- **状态流程文档**: 新增 `docs/job_state_diagram.md`，包含完整的 Mermaid 状态图

### 🐛 问题修复

- **缓存绕过逻辑**: 修复 `max_age=0` 时仍可能走缓存的问题
- **取消后重启失败**: 修复取消任务后再次点击"开始检测"无响应的问题
- **旧任务干扰新任务**: 新任务提交时会自动取消旧任务，避免状态混乱
- **日志旧标记混淆**: "Checking: ..." 日志现在会过滤掉旧的 `【...】` 标记
- **重复方法定义**: 修复 `job_manager.py` 中 `submit_job` 方法重复定义的问题
- **超时参数传递**: 修复前端"请求超时"设置未传递到订阅下载阶段的问题

### 🎨 UI/UX 改进

- **按钮防抖**: "开始检测"按钮点击后变灰并显示加载动画，防止重复点击
- **日志自动清理**: 开始新检测时自动清空旧日志
- **结果区域隐藏**: 开始新检测时自动隐藏之前的下载链接区域
- **设置项描述优化**: 改进"跳过关键词"、"缓存时间"、"请求超时"的中文描述

### 📝 文档

- 新增 `docs/job_state_diagram.md` - 任务生命周期状态图
- 新增 `CHANGELOG.md` - 版本变更日志

---

## [1.0.0] - 初始版本

- 基于 Clash Meta (Mihomo) 的 IP 风险检测
- FastAPI 后端 + Tailwind CSS 前端
- Docker 一键部署
- 智能缓存与任务队列
